# Newly ignored going forward; historical node_modules/dist tracked before this
# file existed are left in place to avoid conflicts (cleanup PR later).
node_modules/
dist/
.mission-control/agents.json
